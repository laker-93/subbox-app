# Cycle log (subbox-app)

Append one line per loop cycle, newest at the bottom. Keep it terse — detail
belongs in `bugs.md` / `ux-notes.md` / `features/*.md`, not here.

Format: `YYYY-MM-DD HH:MM | <journey/area explored> | <outcome>`

Outcome is one of: `verified` (matched docs, no issues), `documented` (new
feature doc written), `bug-fixed` (commit made, see bugs.md), `ux-improved`
(commit made, see ux-notes.md), `logged` (issue found, not fixed), `blocked`
(couldn't proceed — e.g. dev stack down; say why).

**Rotation (do this when you touch this file):** this log is re-read on every
turn of every cycle, so its length is a direct, recurring token cost. Keep only
the most recent ~12 cycles here. When it grows past ~15 entries, move the oldest
block **verbatim** to `log-archive.md` (which the loop never reads — inert
history, same pattern as `directives-archive.md`) and leave a one-line note that
you rotated.

<!-- entries start below -->
(rotated 2026-08-01: moved 2026-07-22..07-24 entries (sync/web-download, external-drive correction, rekordbox metadata+full upload, filebrowser/serato closeout, favorite-button investigation x2) to log-archive.md)
2026-07-25 | resumed abandoned uncommitted work from a crashed cycle (scripts/qa/settings-journey.mjs, and a bugs.md OPEN entry + issue #39 already filed/committed 2026-07-24 09:26) — Step 1.3: re-verify + fix OPEN bug "Export Settings backup writes no file until the whole app is quit (Electron)" | bug-fixed. Confirmed still reproducible isn't needed — root cause was already pinned in the prior cycle's writeup (blob+anchor pattern never completes under Electron's `downloadURL`-based main process, unlike the proven `window.api.utils.download` IPC path used by song-download). Implemented the suggested fix: `onExportSettings` now branches on `isElectron()` and routes through `window.api.utils.download` with a `data:application/json;base64,...` URL + explicit filename; added a `will-download` handler on the main window's session (`src/main/index.ts`) to name the file via `item.setSavePath` (a `data:` URL has no filename of its own); preload's `download` IPC now forwards an optional filename. Web build untouched (still blob/anchor). typecheck clean, lint clean on the 3 changed files (whole-repo `pnpm lint-code` has 6 pre-existing errors, all in unrelated `scripts/qa/*.mjs` scratch drivers). Rebuilt via `electron-vite build --mode development`, re-ran `scripts/qa/settings-journey.mjs` live: export file now appears in ~300ms while the app keeps running (valid JSON, 53083 bytes) — previously absent even after 60s of polling, only appeared on app quit. Committed the recovered driver script alongside the fix. Moved bugs.md OPEN→Closed index, full writeup → bugs-archive.md, features/settings.md written, README Settings row checked off. Code fix committed separately (73b66cc4, Closes #39) from the journal-only commit (docs/qa isn't tracked in sync on development, so bundling them breaks the open-pr.sh cherry-pick — code-only commits are the pattern to follow going forward). PR https://github.com/laker-93/subbox-app/pull/40.
2026-07-25 | coverage: last remaining `[mixed]` README row, "Action required / no-network states" (pymix-qa checklist fully closed out, so stayed on the client side per the alternation rule) | verified `/no-network`, no bug; found + logged 1 latent dead-code finding. New driver scripts/qa/no-network-journey.mjs: launch 1 logs in normally and reads the persisted server URL; launch 2 relaunches with the saved session but Playwright-intercepts+aborts every request to that host (route.abort — a real network-level failure, no shared docker container touched) to force useServerAuthenticated's real retry-then-redirect path. Confirmed live: 1 retry (500ms) then navigation to /no-network (i18n renders as "Server unavailable / Couldn't connect to this server" — NOT literally the words "no network", a driver-locator gotcha not a product issue), full app chrome (sidebar/player) stays visible around it by design (NoNetworkRoute is mounted outside AuthenticationOutlet), saved server/credentials are NOT cleared (verified via direct localStorage read), and clicking Retry + un-blocking the host fully recovers the app (playlists reload, no re-login). Then, closing out the row's other half, confirmed `/action-required` (AppRoute.ACTION_REQUIRED) and its whole component tree (action-required-route.tsx + container + ServerCredentialRequired/ServerRequired) are 100% dead — zero references to the route constant anywhere outside its own enum declaration, never mounted in app-router.tsx's <Routes> tree (unlike its sibling NoNetworkRoute, which IS lazy-imported and mounted), and the scenarios it was built for (no server / no credential) are instead fully handled by AppOutlet rendering LandingPage+PymixAuthModal directly. Logged as a latent/no-issue bugs.md entry (same pattern as the syncTracks/serato-playlistIds dead-surface findings) rather than deleted — this looks superseded (not paused-pending-a-caller), so removal is a 4-file cleanup call outside the small-fix bar, not something to do speculatively. No product code change. Writeup: features/no-network-and-action-required.md. Checked off the row in README — remaining unchecked rows are now all `[upstream]` (login/servers, home/explore, album artists, folders, favorites (blocked), radio).
2026-07-25 | Step 1 (no directives/likely-fixable bugs OPEN; picked the flagged "likely a safe small fix" ux-note) — wishlist header "+" button had no accessible name | ux-improved. Added `aria-label={t('action.addToWishlist', {postProcess:'sentenceCase'})}` to the ActionIcon in `wishlist-header.tsx`, reusing the existing "add to wishlist" string (already the modal title) — no new copy, scoped to just this one button, not the app-wide icon-only-header pattern. `pnpm lint-code`/`pnpm typecheck` clean (fixed a prettier-format warning on the new line with `prettier --write`). Rebuilt via `electron-vite build --mode development`, verified live: `getByRole('button', {name:'add to wishlist'})` now resolves (previously 0 matches). Moved the ux-notes.md entry to IMPROVED (full text → ux-notes-archive.md, one-line index added), refreshed features/wishlist.md's "Driver gotchas" note. `wishlist-journey.mjs` driver itself left unchanged (still anchors via sibling, not role query) — out of scope for this fix. No pymix container touched. Committed to `claude/continuous-ux` in feishin-qa; PR https://github.com/laker-93/subbox-app/pull/43.
2026-07-26 | resumed abandoned uncommitted work found at cycle start: an onBlur-ordering fix in create-wishlist-modal.tsx + a new driver scripts/qa/wishlist-bulk-and-sheet.mjs, no journal trace (a crashed cycle) — coverage: wishlist README's remaining sub-flows (bulk actions, parse-link, sheet-status badge) | bug-fixed. First cleaned up 2 leftover scratch wishlist items ("QA Bulk A/B ...") the crashed cycle's Part 2 had created but never deleted (confirmed via docker logs pymix — reconcile loop was warning on them every minute) via a throwaway direct-API cleanup script. Then ran the recovered driver live: sheet-status error badge/tooltip matches server verbatim (this account's real linked sheet is broken — deleted/unshared); bulk mark-downloaded + bulk delete each fire one PATCH/DELETE per selected row and both rows leave client+server cleanly; parse-link Part 3 FOUND + the uncommitted fix FIXED a real bug (filed issue #44) — Link TextInput's onBlur={handleLinkBlur} was set before spreading {...form.getInputProps('link')}, whose own validation onBlur silently overwrote it (JSX spread order), so pasting a link and blurring never prefilled Artist/Title. Re-verified live post-fix: a pasted YouTube link now correctly fills artist/title. typecheck + lint clean on the one changed file. Filed+committed the issue-link journal edit immediately (per Step 3), then a separate fix commit (Closes #44), then committed the recovered driver script; opened PR #45 for the fix commit specifically (not HEAD, since journal/driver commits stacked on top). Also found (but did NOT run or commit) an unrelated leftover scratch tool scripts/qa/_reset-account.mjs (mtime predates today's work) that mass-deletes every track/playlist in the shared test account via the real UI — looks like unused, unreviewed tooling from an even earlier cycle; flagging here rather than running it blindly. Updated README wishlist row + features/wishlist.md with what's now verified; still unchecked: collection-link create, inbox items, full Google-Sheet sync round-trip, match-youtube.
2026-07-27 01:35 | Step 1 (no directives/likely-fixable OPEN bugs; pymix-qa's only unchecked row, `/serato/import`, is a real 3-cycle-running blocker needing a from-scratch crate-fixture generator, too large for this cycle — deferred, not re-attempted) — self-directed regression sweep via `test-sync-plan-matching` skill, whose feature doc (`sync-plan-classification.md`) turned out to not exist at all despite the skill referencing it | documented + verified, no regression. Confirmed the fix branches (pymix change A, subbox-app changes B-E) are already squash-merged into `development`/`main` HEAD (grep for `matched_subbox_ids`/`watchPaused`/`subboxDir`/recursive `walk`, all present; old branches predictably not literal git ancestors). Built `../feishin` development fresh (electron-vite dev mode). Found + documented real fixture drift: the test plan assumes the old "Kodzo" playlist / ~873-track library, both gone since `test260526`'s 2026-07-25 container recreation (pymix-qa issue #41) — now 30 "QA Load Test NN" playlists and an 18-track local library. Resolved a present track manually (playlist 01, 1/85 overlap) and ran Test A/1 (duplicate-subbox_id playlist, 3x same song) live: PASS (3 present, 0 missing). Test B/2b (recursive-walk sweep, no fixture) also PASS via every run's clean 18-track scan. Scratch "QA Dup Test" playlist deleted after. Also noted (not fixed) an intermittent ~50% driver-only session-bootstrap flake, likely first-launch cold-start timing, not a product bug — logged in the new doc's "Driver gotcha" section for whoever touches the driver next. No code change; pymix container not touched. Checked off the enhancement in README's Sync flows row.
2026-07-29 | Step 1: found this worktree mid an abandoned, uncommitted, undocumented multi-day investigation at cycle start (`_debug-*.mjs`/`_fav-click-test*.mjs` scratch scripts dated 2026-07-26 through today, plus a temporary `console.log` left in `favorite-column.tsx`, no journal trace — a chain of crashed cycles chasing a favorites bug). Needed node@20 on PATH (`export PATH="/opt/homebrew/opt/node@20/bin:$PATH"` — not on PATH by default in this shell despite `.zshrc`) to build/run at all | logged, not fixed. Resumed and drove the recovered `favorites-row-toggle-v2.mjs` (renamed to `favorites-row-toggle.mjs`): confirmed a real, reproducible, fully silent bug — the row-level favorite heart icon in table lists (distinct from the player-bar button, issue #38) does nothing on click (no network request, no toast, no state change). Root-caused as far as: `getServerById(item._serverId)` returns undefined (the row's `_serverId` doesn't match the single entry in the live `serverList`, confirmed via a direct `localStorage` dump), so `createFavorite`/`deleteFavorite` throw before any HTTP call. Could NOT pin down why the row's `_serverId` goes stale (ruled out IndexedDB persistence and multi-server mixups; suspect a boot-time race in `pymix-auth-modal.tsx`'s reauth vs an early session-resume path, not confirmed) — logged OPEN rather than guess a fix, per the conservative bar. Filed issue #53, committed immediately. Reverted the temporary debug `console.log`, deleted ~15 throwaway `_debug-*`/`_fav-click-test*`/`_fav-serverid-check*`/`_fav-plain-test`/`_fav-toast-check` scratch scripts, kept+cleaned the one real driver (`favorites-row-toggle.mjs`), committed separately. Left `_reset-account.mjs` untouched (already flagged risky by a prior cycle, not mine to run or delete). No product code change (debug log reverted, no fix). No pymix container touched.
2026-07-31 | resumed abandoned uncommitted work found at cycle start (third time on this same issue #53 thread): an untested `queryClient.removeQueries()` candidate fix in app-menu.tsx's handleLogOff, 5 leftover `[QA-DEBUG]` console.logs, 7 untracked `_probe-*.mjs` scratch scripts, all dated today, no journal trace — coincides with the last two nightly runs both failing outright (`API Error: Connection closed mid-response` on 07-30, immediate `Execution error` timing out the full 30min gtimeout on all 3 cycles on 07-31), unrelated to this repo | logged, not fixed. Fixed a real driver bug first: favorites-row-toggle.mjs's manual mouse move/down/up click simulation doesn't reliably register on the Mantine ActionIcon (0/2 trials fired anything); replaced with Playwright's own `locator.click({force:true})`, verified via className diffing that this now reliably registers. Re-ran the corrected driver: confirmed the original silent no-op is real (click registers at the DOM level, favorite-toggle logic never completes, no network call) — previous cycles' diagnostics were sound despite the flaky click helper. Investigated the candidate fix and could NOT verify it: `forceFreshLogin()` bypasses the real "Log off" UI path entirely (wipes localStorage directly), so it never exercised the patched code; re-tested via the real UI logoff, found the freshly-logged-in session's favorite button already showed `isMutatingFavorite: true` before any click in the new session (stuck mutation-cache theory, unconfirmed) — and separately found `_serverId` can be stale on a session's very first load, not just across a logoff cycle, ruling out cross-session survival as the sole trigger. Reverted the unverified fix and debug logs; deleted the 07-31 scratch probes. Wrote up all of this in bugs.md's issue #53 entry with concrete next steps (inspect the live mutation cache; trace the boot-time serverId/first-fetch race). Committed the driver fix separately from this journal update. Issue #53 stays OPEN. No pymix container touched.
2026-08-01 | Step 1 (fourth cycle on issue #53's thread — the only bugs.md OPEN entry not flagged "needs a design call") — attempted next step (2) from the prior update: trace the boot-time race between currentServer resolving and the first song-list fetch | logged, not fixed. Instrumented auth.store.ts (setCurrentServer, persist.merge) and both item-list loader hooks' queryFn with temporary timestamped console.logs; drove a scratch Electron probe through forceFreshLogin+login+`/library/songs`. Found no ordering race in `setCurrentServer`/`persist.merge` (2/2 trials consistent — fresh re-login correctly reuses the prior session's server id via the remembered-login fallback). More useful: discovered neither loader's queryFn ever fires for the first page — `getOptimizedListCount` (utils-list-count.ts:60-68) fetches page 1 itself and primes the query cache directly via `client.setQueryData`, so the loader hook's own queryFn is bypassed entirely. This wasn't previously documented and means prior/future boot-race traces need to instrument that function too, not just the loader hooks, or they'll wrongly conclude nothing fetched. Did not reach the stuck-mutation-cache theory (item 1) this cycle. All instrumentation reverted (`git diff` clean before committing) and the scratch probe script deleted — nothing left uncommitted. Wrote the finding into bugs.md's issue #53 entry. Issue #53 stays OPEN. No pymix container touched.
2026-08-01 (later) | Step 1 (fifth cycle on issue #53's thread) — attempted next step (1), the stuck-mutation-cache theory, plus a broad clean repro sweep | logged, not fixed, not reproduced. Instrumented create-favorite-mutation.ts's lifecycle callbacks + exposed queryClient on window (both reverted, git status clean before ending). Ran 6 varied trials (4 fresh-login probes inspecting `getMutationCache().getAll()` directly, 1 full favorites-row-toggle.mjs add+remove run, 1 plain-session-resume probe) — all 6 clean: real HTTP star/unstar calls fired every time, mutation cache never got stuck (always empty before, one `success` entry after), serverId always matched serverList. Stuck-mutation-cache theory: ruled out. Also chased down the driver's own "row still present after re-navigation" observation from the full-driver run — confirmed via direct `docker exec navidrometest260526 sqlite3` against the annotation table that the unstar genuinely persisted server-side (`starred=0`); a follow-up probe waiting past the query layer's 10s staleTime showed the row correctly disappears on a real refetch — not a bug, just caching, the driver's own re-nav timing was inside the staleTime window. 6/6 clean this cycle across both remaining open theories; not closing #53 yet given the original 07-29 discovery's 3/3 concrete repro with a direct localStorage mismatch dump — flagged for whoever resumes that a larger trial count (15-20+, matching sibling issue #38's bar before it was closed not-reproducible) is the natural next step. All scratch probes deleted, all debug instrumentation reverted. Issue #53 stays OPEN. No pymix container touched.
