# Cycle log (subbox-app)

Append one line per loop cycle, newest at the bottom. Keep it terse — detail
belongs in `bugs.md` / `ux-notes.md` / `features/*.md`, not here.

Format: `YYYY-MM-DD HH:MM | <journey/area explored> | <outcome>`

Outcome is one of: `verified` (matched docs, no issues), `documented` (new
feature doc written), `bug-fixed` (commit made, see bugs.md), `ux-improved`
(commit made, see ux-notes.md), `logged` (issue found, not fixed), `blocked`
(couldn't proceed — e.g. dev stack down; say why).

**Rotation (do this when you touch this file):** this log is re-read on every
turn of every cycle, so its length is a direct, recurring token cost. Keep only
the most recent ~12 cycles here. When it grows past ~15 entries, move the oldest
block **verbatim** to `log-archive.md` (which the loop never reads — inert
history, same pattern as `directives-archive.md`) and leave a one-line note that
you rotated.

<!-- entries start below -->
2026-07-11 | reconcile (backfilled pymix issues #23 #24 for 2 OPEN bugs) + coverage: Library Songs (/library/songs "Tracks") client journey | verified list render (78 server tracks, sortable cols, pagination) + play-from-list (audio streaming, on scratch + real "1 on 1" tracks); Favorites list renders. Hardened songs-favorites-journey.mjs (ag-grid 0-height role=row → header-Play + coord-dblclick; retry finder) + new _probe-fav.mjs. Surfaced OPEN anomaly: player-bar favorite button appears inert for now-playing song (0 star.view reqs on click, state never reflects favorited) — logged bugs.md OPEN, NOT filed as issue/fixed (only seen under Playwright bare-out/main launch, flaky finder; must confirm in real pnpm dev first — cf. sync.md getName artifact). Checked off Songs; Favorites left partial. No commit (no verified product fix). Writeup: features/songs-browse-and-play.md.
2026-07-11 | watch-vs-download concurrency — verify user's UNCOMMITTED main-checkout fix (watchPaused defer + stream.pipeline in sync/index.ts) | verified end to end via new driver scripts/qa/watch-download-concurrency.mjs (IPC-timed, QA_APP_ENTRY→../feishin build). Small (Kodzo, tracksExported=1, ~0.7s) and LARGE (emptied dev library → __ALL__, 24 tracks, 11.4s) downloads: watcher ticked every ~800ms before + resumed immediately after, but ZERO scan/upload ticks during the download window (~14 polls would normally fire in the 11.4s case); download resolved cleanly, no ECONNRESET/EPIPE/hang. Dev library backed up + restored to 18-file state. No code change (verifying user's fix, not fixing). Writeup: features/watch-download-concurrency.md. New reusable skill test-watch-download-concurrency.
2026-07-13 09:05 | user-directed: upload 500 tracks from subbox/music via watch-dir + fire a LARGE download DURING the upload (new driver scripts/qa/upload-download-concurrency.mjs) | upload works (run1: 465/465 drained clean, no errors; 20-probe 18/18) AND download-alone works (tracksExported=30). BUT genuine overlap (download fired at 27/478 mid-upload) FAILED — filebrowser returned 500 `input/output error` (EIO) on ~20 uploads + the zip GET; upload stalled 459/478. ROOT CAUSE = host disk exhaustion, NOT a subbox bug: /System/Volumes/Data at 99% (6.5G free); Docker-Desktop-on-mac backs the VM disk with a sparse file on that volume, so ~12G of combined writes (5.9G upload into /watch + the download's library zip) exceeded host headroom → EIO (container df misleadingly showed 17.5G free). pymix logic all 200 OK (sync_plan, sync_playlists, zip export); change C coordination held (upload poll ran, then zip-prep ran). Coexistence itself already proven at small scale (Test C earlier today). Left 458 files/5.9G stuck in test260526 /watch (import blocked by EIO) — NOT cleaned (live container, needs confirmation). No code change. Blocked on env disk space for a clean large-scale concurrent pass.
2026-07-13 09:10 | follow-up: freed VM space (deleted 458 stuck /watch files + stale zip) then re-ran the LARGE concurrent test with a tag-stripped fresh source (ffmpeg -map_metadata -1 → guaranteed-new uploads) | PASS — 300/300 uploaded (4G) drained to idle, large __ALL__ download resolved cleanly (tracksExported=30), download fired mid-upload at 25/300 and the upload genuinely progressed 25->300 across the 54.2s download window (274 active upload ticks inside it), zero EPIPE/ECONNRESET/upload errors. CONFIRMS the prior failure was purely host-disk EIO, not a coordination bug: change C safely serializes (download awaits the in-flight upload poll, then streams) and both complete. Cleaned up all test artifacts (container /watch + host scratch/backup); host free 5.9G->10G. Dev library restored to 18. NB earlier runs' ~900 subbox/music uploads did import into test260526's real library (expected — user asked to upload 500); the 300 stripped junk tracks were deleted from /watch before import so the library was NOT polluted by them.
2026-07-13 09:37 | execute testplan sync-plan-false-missing.md (changes A–E) on local dev stack — fix branches built & running (pymix add-subbox-id-recovery-scripts w/ matched_subbox_ids ×3; ../feishin fix/sync-plan-false-missing-tracks) | verified — ALL PASS. A/1 dup subbox_id (QA Dup Test, Sammy Virji ×3): present 3 / MISSING 0. B/2 no-album-folder (Pat Martino relocated <artist>/<title>, restored after): MISSING 0, both found via recursive walk. B/2b sweep: 18 tracks scanned (17 w/ subboxId), Kodzo 9/9 present, no crash. C concurrency: download resolved tracksExported=1, 0 watcher ticks during window, resumed (6 after). D isolation: FOUND+FIXED build trap — the pre-existing out/main/index.js was compiled in the WRONG mode (subboxDir="subbox" → scan read shared subbox/music=873); rebuilt `electron-vite build --mode development` → subboxDir="subbox-dev", scan now 18=subbox-dev/music, isolated. E observational: change E named the exact corrupt file (Various Artists/Wipeout 3/13 - Sasha - Xpander.mp3, CorruptFileError — the 1-of-18 without a subboxId), no longer silent. Library-state note: dev now has all 9 Kodzo present (plan's 1-missing Sammy Virji baseline superseded). No code change (executing test plan). QA Dup Test playlist left on test260526 (idempotently deleted next make-dup run).
2026-07-13 15:10 | coverage: full-page Search (/search/:itemType) client journey — new driver scripts/qa/search-journey.mjs (deep-link `?query=` + real tab-button clicks; live-typing dropped as the collapse-expand magnifier is flaky to automate and SearchContent reads ?query= directly) | verified — Tracks "Hamdi" 4 rows incl. "Damager (Hamdi Edit)"; Albums tab 2 Hamdi albums; Artists tab renders artist card(s); no-match `zzqqxnomatchzz` → grid correctly empty, no crash/error-boundary; empty query → full library list, no crash. Match counts cross-checked vs live Navidrome search3. Rebuilt the dev-mode Electron build first (out/main was stale prod-mode subboxDir="subbox" from 07-10 → now subbox-dev). Distinguished the full-page search from the sidebar cmdk command palette (separate surface). No product fix. Logged 2 ux-notes OPEN (needs-design-call): (a) no "no results" empty-state message on zero matches; (b) uncontrolled SearchInput text goes stale vs results when query changes via nav (not via typing). Writeup: features/search.md. Checked off Search in README.
2026-07-13 21:30 | regression sweep + drift repair: Library → Artists (artists-browse.md, verified longest-ago client doc) | verified — re-drove scripts/qa/artists-journey.mjs (rebuilt dev-mode Electron first): grid 33 cards, artist cards → /library/album-artists/:id (22 album-artist links, 0 artist-detail anchors), Hamdi detail w/ Play + Album/Top-songs sections + 7-album discography + 3 track rows, top-songs & discography sub-routes no error, Play → real Navidrome stream (audioPaused=false, currentTime advancing). Repaired journal drift flagged in prior log: (a) checked off "Library — artists" README row (was unchecked despite doc verified 07-10); (b) restored the ux-notes "role-only artists → empty detail" entry the doc cross-references but which was lost in a rebase — re-confirmed LIVE (Marco Masis: native /api/artist albumCount=1/songCount=1 but Subsonic getArtist albumCount=0/albums=0 → empty album-artist detail; album-artist index 228 vs native list ≥400). Refreshed artists-browse.md w/ re-verify note + grown counts. No product code change (needs-design-call friction; verification/drift cycle) — no commit/PR.
2026-07-13 20:20 | coverage: Library → Genres (/library/genres, detail) client journey — new driver scripts/qa/genres-journey.mjs (grid → first-card nav → deep-link a rich genre for detail/toggle/play, forcing genreTarget deterministically) | verified end to end, NO bug. Grid renders (18 cards in DOM, 63 genres live). First-card genre ("Dance") deliberately skipped for content checks (only 1 song/1 album) → deep-linked "Electronic" (7bLYq0Np81m1Wgy5N31nuG). Track mode: badge 146 + 13 rows (== live 146 songs); toggle Tracks→Albums flips store+UI, album view badge 60 + 24 cards (== live 60 albums); toggle back to Tracks. Playback: double-click a track row → real Navidrome stream (id LD0Qfpnu7EbSAjY, "Kai Whiston — Quiet as Kept, F.O.G."), audio.paused=false, currentTime advancing. Root-caused two red herrings so they aren't re-chased: (a) genreTarget is a GLOBAL persisted setting (default track) that carries across launches — must be forced deterministically; (b) ag-grid data rows report height=0 in this Playwright build, so a row double-click must aim ~18px below box.y (same quirk as songs-browse). Also: header Play is icon-only (no aria name) and opens a Play/Add-next/Add-last popover, not a direct play — app-wide pattern, not genre-specific. Writeup: features/genres-browse.md. Checked off Genres in README. No product code change; no PR (verification/coverage cycle). Journal drift noted for a future cycle: README "Library — artists" row is unchecked despite artists-browse.md being verified 2026-07-10, and that doc references a ux-notes "role-only artists" entry that is no longer present in ux-notes.md (likely lost in a rebase).
2026-07-14 09:15 | coverage: Delete a track (`[subbox]` row, never driven) — new driver scripts/qa/delete-track-journey.mjs, driven live on purpose-made scratch tracks (watch-dir imported, SUBBOX_ID-stripped so pymix minted fresh ids) | bug-fixed — happy path VERIFIED correct end to end (DELETE /track 200 success:true → file + empty artist/album dirs deleted from disk, beets row gone, pymix map/meta rows gone, Navidrome dropped song+album ~10s; precisely scoped — deleting 1 of a 2-track album left the sibling fully intact). FOUND + FIXED a silent failure: pymix reports a failed delete in the BODY with HTTP 200 (`success:false`), but the client declared the response schema `deleteSong = z.null()` and only checked `res.status !== 200` → **success toast for a delete that never happened**. Repro'd faithfully via a realistic beets/Navidrome desync (`beet rm -f subbox_id::<id>` — drops the beets row, leaves file+Navidrome entry): before, success toast + file still on disk; after the fix (schema captures {success,results}; controller throws on success:false), same flow shows "Error / Failed to delete the track" and the track correctly stays. Happy-path regression re-checked post-fix (1 request, success toast, really deleted). typecheck clean, lint clean on changed files. Issue #18, PR #19. Also logged, NOT fixed: (a) ux-note — a deleted track stays in the list ≥50s (client invalidates immediately, Navidrome only rescans ~5-10s later, so the refetch races and nothing refetches again; clears on re-navigation) — needs a design call; (b) pymix #30 — DELETE /track commits its DB-row deletion BEFORE the file delete, so a failed delete orphans the file with no pymix mapping (confirmed live: map=0 with file still present). Label gotcha for future cycles: the menu item reads **"Delete track"**, not "Delete song" (i18n key is action.deleteSong). All scratch cleaned up; pymix container NOT restarted. Writeup: features/delete-track.md.
2026-07-14 | coverage: Wishlist (`/wishlist`, `[subbox]` row unchecked on BOTH journals) — new driver scripts/qa/wishlist-journey.mjs, driven live on a purpose-made scratch item (test260526) | verified — all 6 steps PASS client-side, no client bug: list renders 5 rows == live GET /wishlist; create via header "+" modal → POST 200 → row appears with no manual refresh (status=wishlist, resolve_state=pending as CreateWishlistRequest.initial_status intends); row click expands WishlistItemDetail; "Mark downloaded" → PATCH → server status + badge both update; "Edit details" → PATCH → album persisted + shown; Delete → confirm → DELETE 200 → gone from list AND server, count back to baseline 5. FOUND + logged (NOT fixed) pymix #31: the background wishlist resolve loop SILENTLY OVERWRITES hand-typed artist/title with unrelated MusicBrainz matches, destroying the user's text (hand-typed items have no raw_note) so download_wishlist.py then hunts Soulseek for the wrong track. Root cause nailed against the real MusicBrainz API: DEFAULT_MIN_SCORE=90 gates on ext:score, which is RELATIVE relevance within the result set, not absolute confidence — a nonsense fielded query returned 35 results ALL scoring 91-100, so the gate only ever rejects an EMPTY result set. Live evidence via POST /wishlist/match-metadata: "Interplanetary Criminal / Yeah Yeah (VIP Mix)" → "Criminal Manne - Yeah" [100]; nonsense → "Wandering Artist - Title" [100]; while the intended typo case "Kahn / Abatoir" → "Kahn - Abattoir" [100] works. Observed end-to-end: my scratch item came back rewritten to "[no artist] - Probe" / album "Vaya Con Tioz", logged by pymix as `resolved 1 … nomatch 0`. Not fixed — the acceptance criterion is a design call (similarity floor vs. artist-exact vs. preserve-original), not a guessed threshold. Also logged ux-note: the header "+" ActionIcon has NO accessible name (aria-label null, tooltip only) while the checkboxes right below it DO set aria-label — inconsistent, and it forces drivers to anchor off the "Offline Wishlist" sibling. Driver gotchas recorded: wishlist table is a plain Mantine <Table> (real row heights — the ag-grid 0-height quirk does NOT apply); label is "Edit details" not "Edit entry"; id field is `wishlist_id` not `id`; the resolve loop mutates scratch text so track items by wishlist_id. Cleaned up a scratch item leaked by an earlier crashed run (count 6→5, back to the user's 5 real rows). pymix container NOT rebuilt/restarted. Writeups: features/wishlist.md + ../pymix-qa/docs/qa/features/wishlist-api.md. Checked off Wishlist in BOTH READMEs.
2026-07-21 | Step 1.3 OPEN bug #23 ("watch uploader restores UI on relaunch but never re-arms watcher") — re-verify before fixing | NOT A BUG, closed. Built a pollution-free relaunch regression scripts/qa/watch-resume-relaunch.mjs (Start Watching → quit → relaunch, empty watch dir so nothing uploads; asserts >=1 [scanning,idle] watch-progress event = watcher genuinely re-armed). Initially wrote the mount-effect re-arm the issue suggested (sync-watch.tsx), typecheck+lint clean, driver PASS — but the NEGATIVE control (same driver on the un-patched base) ALSO passed, with events arriving BEFORE navigating to Sync→Watch. Root-caused: app.tsx already has an "Auto-resume watch directory on app launch" boot effect (added 2026-06-05, commit 81fd618d — 6 weeks before #23 was filed) that re-invokes sync:start-watch once fbToken hydrates, independent of navigation. The #23 report inspected only sync-watch.tsx and missed it; the "Stop Watching" UI on relaunch is truthful (watcher IS running). REVERTED my redundant sync-watch.tsx patch (would double-invoke start-watch), closed issue #23 not-a-bug, moved bugs.md OPEN→Closed. Confirmed dev mode bypasses the single-instance lock (index.ts:898) so launches are separate PIDs (no cross-process leak). Kept the regression driver + documented the verified behavior in features/watch-upload.md ("Relaunch auto-resume"). No product code change. Driver is a clean pass/fail but stays a plain scripts/qa/ regression (not skill-ified this cycle).
2026-07-21 | drive the client-side delete journey to close pymix's outstanding #30 gap (DELETE /track reorder, PR #36 merged — the live happy-path re-drive deferred in yesterday's pymix reconcile) | PASS end-to-end. Minted a fresh scratch track via the watch-dir uploader (upload-music-dev flow, `QA Delete Me 20260721`, SUBBOX_ID stripped → new subbox_id e4526cc0…), then deleted it through the real song context menu (scripts/qa/delete-track-journey.mjs, default /search/song route). Client behaved correctly: found the row, "Delete track" menu → confirm modal → DELETE /track 200 success:true (single batched call), success toast, toast/body agreement ok, row gone after re-navigation. Server-side confirmation (all stores clean, no orphan) is in the pymix-qa journal — see ../pymix-qa/docs/qa/{log.md,features/track-delete.md,bugs-archive.md}. Re-confirmed the two known, already-logged friction points are unchanged (not new bugs): (1) the row does NOT clear on its own on the /search route within 50s — the ux-note "deleted track stays visible" (search query key isn't invalidated + Navidrome async rescan race); it DOES clear on re-navigation. No client bug, no code change (verifying a merged pymix fix). Refreshed features/delete-track.md with the re-verify note.
2026-07-22 | correction cycle (outside the loop, human-directed): this worktree had a local-only, never-pushed chain of External Drive tab work (issue #27 diagnosis + PR #28) that turned out to misread the feature's design | bug-fixed, prior diagnosis reversed. Confirmed with the maintainer that the External Drive tab's drive/USB folder is compare-only BY DESIGN — "Download Missing Tracks" always extracts into the app's own Subbox library, same as a normal playlist download, so the user can add tracks to Rekordbox and re-export to the drive from there. An earlier local cycle read the missing drivePath wiring as a routing bug, filed issue #27, and opened PR #28 to route downloads to the drive instead — that fix worked against the feature's actual design and was closed unmerged. The real defect was the UI copy ("...onto the selected drive folder" was simply false). PR #29 (merged) reworded the copy and added the same "Include Rekordbox XML" export + Show Music/Show Rekordbox XML reveal buttons the regular Download tab has. Re-verified live via new driver `scripts/qa/external-drive-rekordbox-xml.mjs`: scratch drive dir stayed empty (0 files, compare-only confirmed), app music folder got 18 real audio files, XML exported, reveal buttons present, clarified copy visible on the select screen. typecheck + lint clean. Checked off External Drive coverage in README. Writeup: features/external-drive-sync.md; bugs.md Closed index updated (issue #27, PR #29, supersedes closed #28). NOTE: this cycle's own journal-only history (issue #25 web-download fix docs, watch-download-concurrency docs, etc.) was found to be local-only on this worktree's prior `claude/continuous-ux` branch and never reached origin — flagged for the user to reconcile separately; not attempted here.
