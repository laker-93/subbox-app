# Cycle log (subbox-app)

Append one line per loop cycle, newest at the bottom. Keep it terse — detail
belongs in `bugs.md` / `ux-notes.md` / `features/*.md`, not here.

Format: `YYYY-MM-DD HH:MM | <journey/area explored> | <outcome>`

Outcome is one of: `verified` (matched docs, no issues), `documented` (new
feature doc written), `bug-fixed` (commit made, see bugs.md), `ux-improved`
(commit made, see ux-notes.md), `logged` (issue found, not fixed), `blocked`
(couldn't proceed — e.g. dev stack down; say why).

**Rotation (do this when you touch this file):** this log is re-read on every
turn of every cycle, so its length is a direct, recurring token cost. Keep only
the most recent ~12 cycles here. When it grows past ~15 entries, move the oldest
block **verbatim** to `log-archive.md` (which the loop never reads — inert
history, same pattern as `directives-archive.md`) and leave a one-line note that
you rotated.

<!-- entries start below -->
(rotated 2026-08-01: moved 2026-07-22..07-24 entries (sync/web-download, external-drive correction, rekordbox metadata+full upload, filebrowser/serato closeout, favorite-button investigation x2) to log-archive.md)
(rotated 2026-08-14: moved 2026-07-25..07-26 entries (Export Settings fix, no-network/action-required closeout, wishlist "+" aria-label, wishlist bulk/parse-link fix) to log-archive.md)
(rotated 2026-08-17: moved 2026-07-27..08-02 entries (sync-plan-matching regression sweep, issue #53 favorites investigation cycles 1-4 + trial-sweep closeout) to log-archive.md)
2026-08-02 | Step 4 coverage: closed the last unchecked-and-substantive checklist row, `[upstream]` Favorites (`/favorites`) row-level add/remove toggle (issue #53's code path, distinct from the player-bar toggle) | documented + driver-fixed + ux-logged. Existing driver `favorites-row-toggle.mjs` had a real bug (not a product bug): captured a row Locator pre-scroll, then scrolled+clicked it — but the songs/favorites table is a `react-window`-virtualized Grid that recycles DOM nodes across scroll, so the click could land on a different track than the one whose title was read (caught live: one run starred "…Copy 3" while the intended target was "…Copy 2", confirmed via direct sqlite query against navidrometest260526; cleaned up the stray star by hand). Fixed the driver to re-locate the row by expected title after scroll-settle and re-verify identity immediately before clicking (throws loudly on mismatch instead of silently mis-clicking). Re-ran clean: add (from /library/songs) and remove (from /favorites itself, no navigation) both fired the correct star.view/unstar.view request against the correct track, server-side annotation.starred confirmed correct both times. Surfaced one real ux-note along the way (not a bug): un-favoriting from /favorites leaves the row listed until a real refetch (React Query staleTime ~10s) because `applyFavoriteOptimisticUpdates`'s SONG branch patches `userFavorite` in place rather than removing the item from the server-filtered starred-only list — confirmed via network trace that a too-soon re-navigation issues zero requests (pure cache), while waiting past 10s triggers a real `starred=true` refetch that correctly drops it. Logged OPEN (design call, same shape as the existing delete-track staleness note), not fixed. Also refreshed the stale "OPEN bug (issue #53)" section in features/songs-browse-and-play.md — #53 was already closed not-reproducible by the prior 6 cycles but the doc hadn't caught up. Checked off the README coverage row. Committed: driver fix + journal (features/ux-notes/README) in this repo's claude/continuous-ux branch; no product src/ changed, so no PR needed. Electron processes launched during driving all closed cleanly (process.exit); no stray processes. No pymix container touched.
2026-08-03 | Step 1 (no directives/PENDING or IN PROGRESS; both journals' OPEN bugs.md/ux-notes.md entries are deliberate design-call/no-issue-by-design items, none flagged likely-fixable; pymix-qa README checklist fully checked, so only feishin-qa had unchecked rows left) — coverage: last 3 unchecked `[upstream]` README rows, Login/servers + Home/Explore + Library-Folders, driven as one journey | documented, no code fix. New driver login-servers-home-folders-journey.mjs. Home and Folders verified working correctly (real carousel content; folder double-click descends into subfolders via folderPath query param). Found `/explore` and `/servers` are both unmounted dead enum values (zero references outside routes.ts/app-router.tsx), falling through to InvalidRoute's 404 gracefully — not a bug. Found `/login` is worse: mounted as a *sibling* of AuthenticationOutlet/AppOutlet rather than nested inside, so it bypasses subbox's real PymixAuthModal login path entirely; logged-out it renders a dead-end page dumping raw internal SERVER_LOCK-config JSON to the screen with no way back. None of these are user-reachable though (nothing in the app links to /login, /servers, or /explore) — same class as the already-documented /action-required dead code, so logged informationally in bugs.md (2 new entries), no qa-bug issues filed, no code touched, per the established policy for this finding shape. Checked off all 3 README rows. Committed docs+driver (a9d6338f). No pymix container touched. Electron process closed cleanly (electronApp.close()), verified no dangling processes after.
2026-08-03 (later) | Step 1 (no directives; both journals' OPEN entries remain deliberate design-call/no-issue-by-design, none newly fixable; pymix-qa checklist fully checked) — Step 4 coverage: last unchecked `[upstream]` row, Library — album artists, driven via its own dedicated nav item (not an Artists-card click, which artists-browse.md already covers by coincidence) | documented, no bugs found. New driver album-artists-journey.mjs: grid (634 album artists, sort control present) → detail (Hamdi, 2 albums/8 tracks, all header elements correct) → all 4 sub-routes unique to this family (songs=9 rows, favorite-songs=1 row, top-songs=3 rows, discography=5 card links) → Play (real audio playback confirmed, currentTime advancing, correct player-bar text). Everything rendered correctly with no errors/empty-states; confirmed by construction that the role-only-artist empty-detail friction (ux-notes.md, native Artists list only) doesn't apply to this clean album-artist index. Checked off the last remaining unchecked README row in either journal — client coverage checklist is now 100% `[x]`; next cycle with no directive/OPEN-fixable work moves to Step 5 self-directed discovery. Wrote features/album-artists-browse.md. No product code change, no commit needed beyond docs+driver. No pymix container touched. Electron process closed via electronApp.close(); verified no dangling processes after.
2026-08-04 | Step 1 (no directives; both journals' OPEN bugs.md entries remain empty, ux-notes.md OPEN entries all deliberate design-call items, pymix-qa checklist fully checked) — Step 4 coverage: last unchecked README row, `[upstream]` Radio (`/radio`) | documented, no bugs found, driver committed. New driver scripts/qa/radio-journey.mjs: create a real internet-radio station pointed at a public Icecast test stream (SomaFM Groove Salad) -> play (confirmed real `<audio>` playback, currentSrc/currentTime/paused all correct, player-bar station-name metadata shown) -> stop -> admin-gated edit/delete permission check -> delete, full round-trip verified against `navidrometest260526` directly (station gone from `radio` table after). First pass surfaced what looked like a permissions bug (created 2 scratch stations, no delete button visible on either) — traced to a stale persisted `isAdmin: false` in a resumed session's `store_authentication`, not a real bug: a fresh login correctly returns `isAdmin: true` (confirmed via direct `/auth/login` curl and the per-user DB's `user.is_admin=1`), and the driver just hadn't forced one. Fixed the driver to `forceFreshLogin()` unconditionally, re-ran clean (3 buttons per row = play+edit+delete, delete works). Also fixed two Playwright strict-mode locator bugs in the driver itself (unscoped station-name text matched both the list row and the player-bar's link-back-to-/radio; edit/delete `ActionIcon`s have no aria-label/title, only a portal-only Tooltip, so `[aria-label*="Delete"]` matched nothing). Manually cleaned up the 2 leftover scratch stations from the earlier stale-session run via a throwaway cleanup script (deleted after use) — confirmed empty `radio` table before ending. Wrote `features/radio.md` including the driver gotchas for reuse. Checked off the README row — **client coverage checklist is now genuinely 100% `[x]`** (a prior 2026-08-03 log entry claimed this already but the Radio row was actually still unchecked — this cycle is the real closeout). No product code change (driver/docs only, no PR needed). No pymix container touched. Electron processes all closed via `electronApp.close()`; verified no dangling processes.
2026-08-04 (later) | Step 1½: no OPEN bugs.md entries with Issue: links to reconcile (all remaining OPEN entries in both journals are deliberate no-issue-by-design/design-call items). Step 1 tier 5: client checklist just went 100% [x] as of the prior cycle, so this was its first self-directed-discovery cycle — regression sweep (least-recently-exercised tier-5 mode on this side), targeting the oldest non-upstream feature doc, `features/playlist-add-and-download.md` (07-10, [mixed]) | verified, no regression, doc refreshed. Found the doc's original fixture (the `qa-scratch` track + "QA Import Playlist 0709") is gone — same root cause as the sync-plan-classification/track-metadata cycles: `test260526`'s Navidrome container was recreated from scratch 2026-07-25 (pymix#41). Rebuilt the client (`electron-vite build --mode development`, needed `node@20` on PATH per the 07-29 note) and re-drove the full journey against a fresh throwaway fixture: added an existing "QA Load Test 01" track (Burial — "UK") to a new playlist "QA Regression 0804" via `add-to-playlist-smoke.mjs`, then ran `sync-smoke.mjs` (QA_DOWNLOAD=1) against it. Confirmed identical behavior to the original writeup end to end: Preview correctly showed Missing(1)/Present(0), download completed ("1 track exported"), local file landed byte-exact to the server copy (485414 B both sides, confirmed via direct `navidrometest260526` sqlite query), `SUBBOX_ID` tag frame present in the downloaded file, prod's shared `subbox/music` folder untouched (970 files, dev/prod isolation holds), and a second Preview flipped to Missing(0)/Present(1) as expected. Same cosmetic 502 console error recurred, still non-blocking. Cleaned up the scratch playlist (direct sqlite delete on `navidrometest260526`, the same test account past cycles have written to directly) and the downloaded file — no fixture left behind this time. No code change (doc-only); no PR needed. Electron process closed cleanly, no dangling processes after.
2026-08-13 | Not a loop cycle — pre-resume audit by the user's interactive session after the runner was paused 2026-08-04→08-13. Recorded here so the next cycle knows why the journal moved without a matching digest; the pymix-qa journal has the mirror entry. (1) **`test260526` no longer exists** — the stack's only user is now `test060826`, and it is the user's own #104/#105 perf fixture (`t104before/after/a2/b2`, `backup`, 97 tracks), NOT disposable — README gained hands-off rules; `.env.ui-snapshot.local` re-pointed at it and login re-verified (HTTP 200). The ~69 mentions of the old account across this journal are historical fixtures, not reusable. (2) **This branch was 7 client releases behind** (1.10.16 vs development's 1.10.23) because `sync-merged.sh` skips a dirty worktree, and three untracked scratch drivers (`_bench-rekordbox-progress`, `_jamendo100-upload{,-v2}`, from the user's 08-06 work) plus an uncommitted `CLAUDE.md` edit had kept it dirty for the whole pause. The `_`-prefix gitignore is now a single `scripts/qa/_*.mjs` glob instead of a per-prefix allowlist that new prefixes slipped past, and the CLAUDE.md commit-trailer rule moved to the workspace CLAUDE.md — which is the file a cycle actually loads, cwd being `subbox-workspace`. (3) **Dropped commit `a7227e1c`** (the Export Settings Electron download fix): its PR #40 was closed *unmerged* by the user as low-priority, but the commit stayed on this branch, so cycles were driving a build containing a declined change, and it conflicted with upstream #72 on the same file — the actual cause of the rebase failure. Preserved on local ref `qa-backup-20260813`. Its bugs.md line said FIXED; moved back to OPEN with the full history, since Step 1½ only re-examines OPEN entries and would never have caught it. (4) Rewrote `features/sync.md` from source for #101/#102/#103 (one download with tracks/XML tick-boxes, web `user_root` gains a `music` segment, web preview is now a manifest while desktop keeps the diff) plus #79/#81/#82 — **source-accurate, NOT driven**, banner-marked; it is now the highest-priority regression sweep here. (5) Added 8 unchecked README rows for pause-era surface (rebuilt Download, mobile Sync, import phases, the whole new `features/invite/`, demo restrictions, Settings About tab, legal pages, analytics-beacon removal). (6) Marked the "Kodzo duplicate" entry OBSOLETE — its fixture died with the old account; a divergence signal seen now is new.
2026-08-14 (later) | Step 1½: no reconcile needed (subbox-app#39 and pymix#40 both still OPEN, as expected; no other OPEN entries carry Issue: links). Step 1: no directives/PENDING; both journals' OPEN bugs.md entries are deliberate design-call/no-issue-by-design or (pymix#123) blocked on a busy shared container — confirmed still busy this cycle too (`pymix`/`player` containers up ~20min on custom `-local` tags, `beetstest060826` freshly recreated by a `/admin/beets/migrate` call ~1min after pymix started — real concurrent user activity, left untouched). Step 4 coverage: picked the highest-priority unchecked `[subbox]` row, "Sync → Download, rebuilt" (#101/#102/#103), desktop half only (client-only, no pymix container change needed) | documented, no bugs found. Rebuilt via `electron-vite build --mode development`. New driver `scripts/qa/sync-download-tickboxes.mjs` against `test060826`'s "Downtempo" playlist (9 tracks, smallest real playlist — `test260526`'s old fixtures are gone). Verified live: both tick-boxes render/default-checked; desktop keeps the full diff (Missing/Already Present/Metadata Updates tabs); unticking "Include tracks" correctly downloads XML-only with the right button label + done-screen copy + no audio files on disk; both-ticked correctly downloads all 9 real tracks + XML with "Show Music"/"Show Rekordbox XML" buttons. Found one real-but-not-a-bug behavior worth documenting: `includeTracks`/`includeRekordboxXml` are plain component state, NOT reset by "Start Over" (only `step`/`plan`/`error`/`downloadResult` are) — so a tick-box choice persists across a new plan within the same visit to the screen; had to explicitly re-check the box mid-driver once this was understood. Scratch downloads (9 mp3s + XML) deleted after. Updated `features/sync.md` (new "Desktop tick-boxes, re-verified 2026-08-14" section, banner narrowed to flag only web+mobile as still undriven) and README (row left unchecked, noted desktop done — web manifest view (#102/#103) and the sub-768px breakpoint (#81) are the remaining sub-steps for a future cycle). No product code change; no PR needed. pymix container not touched (read-only `/sync/plan`+`/sync/playlists` calls only, no rebuild/swap). Electron process closed via `electronApp.close()`; verified no dangling processes after. Rotated log.md (moved 2026-07-25..07-26 entries to log-archive.md).
2026-08-17 | Step 1½: no reconcile needed (subbox-app#39 still OPEN as expected, no other OPEN entries carry Issue: links). Step 1: no directives; no likely-fixable OPEN bugs.md/ux-notes.md entries in either journal (all deliberate design-call/no-issue-by-design). Step 4 coverage: closed out the remaining two sub-steps of the highest-priority unchecked `[subbox]` row, "Sync → Download, rebuilt" (#101/#102/#103 web manifest view) and its sibling row "Sync below the 768px breakpoint" (#81) | documented, no bugs found. Started `pnpm dev:web` (port 4343, the only origin in pymix's local CORS allowlist; needed `node@20` on PATH). New driver `scripts/qa/web-sync-manifest.mjs` against `test060826`'s "Downtempo" playlist: confirmed the web build renders a manifest (badges + flat track list, no diff tabs) not a diff, both tick-boxes present/default-checked, the web-only extraction-path field appears and gates the download button correctly, XML-only download fires a real `subbox_rb_export.xml` browser download event, and re-ticking tracks+XML (after the same "Start Over" remount-retry gotcha as the desktop driver) fires a real `music.zip` download event — zip byte contents already verified server-side in pymix-qa's 2026-08-14 cycle, this confirms the client wiring. New driver `scripts/qa/mobile-sync-breakpoint.mjs` (Chromium 400×800, well under the 768px media query): `#mobile-layout` mounts, `ModeToggle` + `MobileSyncPlaceholder` render correctly on switching to Sync, "Back to Library" round-trips correctly, and the real Sync tabs don't leak through underneath. Noted (not filed, cosmetic): every web dev-server run 404s on `GET /settings.js` — `index.html` unconditionally references it, real deploys generate it from `settings.js.template`, `pnpm dev:web`'s dev server just doesn't serve one; no functional impact observed. Updated `features/sync.md` (new "Web manifest view" + "Mobile breakpoint" sections, banner now reflects nothing left source-read-only) and checked off both README rows. No product code change; no PR needed. `pymix` container not touched (read-only `/sync/plan`+`/sync/playlists` HTTP only). `pnpm dev:web` process killed cleanly at the end of the cycle; no dangling processes.
2026-08-17 (later) | Step 1½: no reconcile needed. Step 1: no directives; no likely-fixable OPEN bugs/ux-notes. Step 4 coverage: pause-era `[subbox]` "Invite funnel" row (landing + create-account entry points; `DemoBanner`/`InviteLockedPanel` out of scope, need a demo session not available in local dev) | bug-fixed. New driver `scripts/qa/invite-funnel-journey.mjs`. Found + fixed a real bug live: `useRequestInvite` inherited the app-wide `mutations.retry:3` default (react-query.ts; confirmed this is baked into every build including `--mode development`, not prod-only, since plain `vite build` always defines `process.env.NODE_ENV` as `'production'` for the bundle) — one click on a rejected email fired 4 requests over ~8s against pymix's `POST /invite-request`, which caps at 5/hour/IP specifically so a real user correcting a typo never hits it (confirmed via `docker logs pymix`). Fixed with a scoped `retry: false` on `useRequestInvite`'s own mutation (same pattern as `share-item-mutation.ts`). Also verified live: server-rejected-but-HTML5-valid email (`a@b`) renders the real inline field error; valid submissions (incl. `dj_software: other` + free text) reach `invite_request_table` correctly; the create-account entry point's invite modal correctly stacks above the auth modal (`zIndex=400`, screenshot-confirmed). Restarted the shared `pymix` container twice mid-cycle (confirmed idle both times) purely to reset its in-memory rate-limit counter between test runs — no image rebuild, no code change on that side. All scratch `invite_request_table` rows deleted after each run (table empty before/after). `pytest`-equivalent here (`pnpm typecheck && pnpm lint-code`) clean for the changed files. Filed issue #105, committed fix (`755ba5d9`, `Closes #105`), opened PR #106. Wrote `features/invite-funnel.md`, checked off the README row. Electron processes closed cleanly each run; no dangling processes.
