# Cycle log (subbox-app)

Append one line per loop cycle, newest at the bottom. Keep it terse — detail
belongs in `bugs.md` / `ux-notes.md` / `features/*.md`, not here.

Format: `YYYY-MM-DD HH:MM | <journey/area explored> | <outcome>`

Outcome is one of: `verified` (matched docs, no issues), `documented` (new
feature doc written), `bug-fixed` (commit made, see bugs.md), `ux-improved`
(commit made, see ux-notes.md), `logged` (issue found, not fixed), `blocked`
(couldn't proceed — e.g. dev stack down; say why).

<!-- entries start below -->
2026-07-09 10:20 | SUBBOX_ID sync directive: read diffs, drove sync/plan preview live (Electron + rebuilt pymix) | logged (1 pymix bug, 1 client ux-note; directive left IN PROGRESS, several sub-steps remain)
2026-07-09 12:05 | root-caused first-click 400 (user-directed follow-up, not autonomous) | verified — not a bug, working as designed (silent pymix session reauth-and-retry); moved ux-notes.md entry from OPEN to RESOLVED
2026-07-09 13:35 | investigated subboxId cache location (continuing SUBBOX_ID directive) | false lead — apparent cache-mislocation bug was a Playwright test-harness artifact (app.getName()="Electron" when launching bare out/main/index.js), not reproducible in real pnpm dev usage; drafted fix, verified it changed nothing observable, reverted; documented in features/sync.md so it isn't re-chased
2026-07-09 14:00 | pymix#15 (dev/prod music folder isolation) merged: rebased, drove real Download & Extract, cache invalidation + pruning (sub-steps 4/5/6) | verified — all working correctly; isolation confirmed live (shared subbox/music untouched at 808 files); bonus real-world confirmation of pymix#22's subbox_id_divergence signal (genuine duplicate playlist track, logged informationally in bugs.md)
2026-07-09 20:23 | SUBBOX_ID sync directive sub-step 3 (untagged-local fuzzy fallback): stripped SUBBOX_ID off real Oleo file, drove Kodzo Preview Download | verified — untagged local fuzzy-matched at similarity 1.000 (via=fuzzy), moved missing→already-present; corroborated OPEN pymix subbox_id_divergence over-fire (count 2→1). Directive now DONE (yt-dlp sub-step split into own PENDING directive). No code fix — verification cycle.
2026-07-09 21:35 | phone/Discord wishlist-import directive (promoted to IN PROGRESS): sub-step 1 seed+resolve | verified sub-step 1 (seeded Aphex Twin - Xtal, resolved, not-yet-owned); sub-step 2 blocked — download_wishlist.py can't resolve navidrome<user>.docker.localhost from Python (not in /etc/hosts; curl's *.localhost handling masks it), /etc/hosts fix needs sudo unavailable in background. Root-caused + resume plan in directives.md. Fix is host/subbox-slskd (out of QA-worktree scope) — logged, not fixed.
2026-07-09 20:40 | phone/Discord wishlist-import directive, sub-step 3 (import→tag→Navidrome) via sanctioned import-half shortcut (Soulseek sub-step 2 still blocked, re-confirmed) | verified — dropped a genuinely-new real audio file (SUBBOX_ID stripped) into test260526's watch dir; pymix watcher debounced+imported it, assigned+physically wrote fresh SUBBOX_ID (1e5002e2-…), Navidrome scanned it in. Writeup: ../pymix-qa/docs/qa/features/watch-dir-import.md. Scratch track left in place for sub-steps 4/5. No fix committed (import path works as-is); logged 1 pre-existing OPEN pymix note (orphaned-downloads-beets-entries).
2026-07-10 02:05 | phone/Discord wishlist-import directive, sub-steps 4 (add-to-playlist) + 5 (download-to-local) | verified end to end — scratch track confirmed in "QA Import Playlist 0709" server-side (Subsonic) + client-side (sync plan); drove Preview Download (Missing 1 / 3.6MB) → Download & Extract → file landed in subbox-dev/music (7→8), byte-exact (3806839B), SUBBOX_ID preserved, shared subbox/music untouched at 808; follow-up preview flipped missing→already-present. Directive DONE (user's import→playlist→download journey fully verified). Soulseek-acquisition half split into its own PENDING directive. No bug — flow works as designed; no code change. Writeup: features/playlist-add-and-download.md.
2026-07-10 06:45 | Soulseek-acquisition directive — all 3 sub-steps in one cycle (Soulseek was logged in & cooperative) | verified end to end — slskd Connected/LoggedIn; download_wishlist.py --max-downloads 3 pulled Aphex Twin - Xtal.flac from peer "Slapper" (82 sources), row flipped wishlist→downloaded; bridged into watch dir (docker cp), pymix beet-imported (beet_id 666) + physically stamped SUBBOX_ID 09d4a6f0-…, Navidrome scan 77→78, reconcile flipped row downloaded→available with linked_subbox_id=09d4a6f0-… (matches stamp). Directive DONE — entire phone/Discord journey now fully verified. No bug, no code change. Writeup: ../pymix-qa/docs/qa/features/wishlist-download-acquisition.md.
2026-07-10 08:20 | Library albums client journey (coverage: grid → album detail → play → Now Playing) + journal hygiene | verified end to end — 8-album virtualized grid, rich album-detail header w/ Play, clicking Play started a real Navidrome stream (audio.paused=false, currentTime advancing), /now-playing queue rendered the 3 played tracks w/ current row highlighted. No bug, no fix. Documented: features/albums-browse-and-play.md (+ two non-bug gotchas: appMode gates the router Outlet so a /library/* deep-link in Sync mode shows the Sync placeholder; /playing is a dead orphaned-enum route, real queue is /now-playing). New driver scripts/qa/albums-journey.mjs. Also removed a stale PENDING Soulseek directive already sitting in DONE (leftover from the 06:45 cycle). Checked off "Library — albums" + "Now playing queue" in README.
2026-07-10 09:30 | yt-dlp cookie-auth PENDING directive (pymix #21 ytdlp_support.py) | verified live (pymix-qa venv, no network/prod) — resolve_cookiefile matrix + opts wiring: a present cookies file reaches yt-dlp opts["cookiefile"] in both consuming services, absent→anonymous unchanged. No bug, no code change. Writeup: ../pymix-qa/docs/qa/features/ytdlp-cookie-auth.md. Authenticated-download outcome is prod-only (real cookies + datacenter IP) — handed to user. Directive → DONE (local scope). Clears the only PENDING directive; next cycle rotates to the coverage checklist.
2026-07-11 | reconcile (backfilled pymix issues #23 #24 for 2 OPEN bugs) + coverage: Library Songs (/library/songs "Tracks") client journey | verified list render (78 server tracks, sortable cols, pagination) + play-from-list (audio streaming, on scratch + real "1 on 1" tracks); Favorites list renders. Hardened songs-favorites-journey.mjs (ag-grid 0-height role=row → header-Play + coord-dblclick; retry finder) + new _probe-fav.mjs. Surfaced OPEN anomaly: player-bar favorite button appears inert for now-playing song (0 star.view reqs on click, state never reflects favorited) — logged bugs.md OPEN, NOT filed as issue/fixed (only seen under Playwright bare-out/main launch, flaky finder; must confirm in real pnpm dev first — cf. sync.md getName artifact). Checked off Songs; Favorites left partial. No commit (no verified product fix). Writeup: features/songs-browse-and-play.md.
2026-07-11 | watch-vs-download concurrency — verify user's UNCOMMITTED main-checkout fix (watchPaused defer + stream.pipeline in sync/index.ts) | verified end to end via new driver scripts/qa/watch-download-concurrency.mjs (IPC-timed, QA_APP_ENTRY→../feishin build). Small (Kodzo, tracksExported=1, ~0.7s) and LARGE (emptied dev library → __ALL__, 24 tracks, 11.4s) downloads: watcher ticked every ~800ms before + resumed immediately after, but ZERO scan/upload ticks during the download window (~14 polls would normally fire in the 11.4s case); download resolved cleanly, no ECONNRESET/EPIPE/hang. Dev library backed up + restored to 18-file state. No code change (verifying user's fix, not fixing). Writeup: features/watch-download-concurrency.md. New reusable skill test-watch-download-concurrency.
2026-07-13 09:05 | user-directed: upload 500 tracks from subbox/music via watch-dir + fire a LARGE download DURING the upload (new driver scripts/qa/upload-download-concurrency.mjs) | upload works (run1: 465/465 drained clean, no errors; 20-probe 18/18) AND download-alone works (tracksExported=30). BUT genuine overlap (download fired at 27/478 mid-upload) FAILED — filebrowser returned 500 `input/output error` (EIO) on ~20 uploads + the zip GET; upload stalled 459/478. ROOT CAUSE = host disk exhaustion, NOT a subbox bug: /System/Volumes/Data at 99% (6.5G free); Docker-Desktop-on-mac backs the VM disk with a sparse file on that volume, so ~12G of combined writes (5.9G upload into /watch + the download's library zip) exceeded host headroom → EIO (container df misleadingly showed 17.5G free). pymix logic all 200 OK (sync_plan, sync_playlists, zip export); change C coordination held (upload poll ran, then zip-prep ran). Coexistence itself already proven at small scale (Test C earlier today). Left 458 files/5.9G stuck in test260526 /watch (import blocked by EIO) — NOT cleaned (live container, needs confirmation). No code change. Blocked on env disk space for a clean large-scale concurrent pass.
2026-07-13 09:10 | follow-up: freed VM space (deleted 458 stuck /watch files + stale zip) then re-ran the LARGE concurrent test with a tag-stripped fresh source (ffmpeg -map_metadata -1 → guaranteed-new uploads) | PASS — 300/300 uploaded (4G) drained to idle, large __ALL__ download resolved cleanly (tracksExported=30), download fired mid-upload at 25/300 and the upload genuinely progressed 25->300 across the 54.2s download window (274 active upload ticks inside it), zero EPIPE/ECONNRESET/upload errors. CONFIRMS the prior failure was purely host-disk EIO, not a coordination bug: change C safely serializes (download awaits the in-flight upload poll, then streams) and both complete. Cleaned up all test artifacts (container /watch + host scratch/backup); host free 5.9G->10G. Dev library restored to 18. NB earlier runs' ~900 subbox/music uploads did import into test260526's real library (expected — user asked to upload 500); the 300 stripped junk tracks were deleted from /watch before import so the library was NOT polluted by them.
2026-07-13 09:37 | execute testplan sync-plan-false-missing.md (changes A–E) on local dev stack — fix branches built & running (pymix add-subbox-id-recovery-scripts w/ matched_subbox_ids ×3; ../feishin fix/sync-plan-false-missing-tracks) | verified — ALL PASS. A/1 dup subbox_id (QA Dup Test, Sammy Virji ×3): present 3 / MISSING 0. B/2 no-album-folder (Pat Martino relocated <artist>/<title>, restored after): MISSING 0, both found via recursive walk. B/2b sweep: 18 tracks scanned (17 w/ subboxId), Kodzo 9/9 present, no crash. C concurrency: download resolved tracksExported=1, 0 watcher ticks during window, resumed (6 after). D isolation: FOUND+FIXED build trap — the pre-existing out/main/index.js was compiled in the WRONG mode (subboxDir="subbox" → scan read shared subbox/music=873); rebuilt `electron-vite build --mode development` → subboxDir="subbox-dev", scan now 18=subbox-dev/music, isolated. E observational: change E named the exact corrupt file (Various Artists/Wipeout 3/13 - Sasha - Xpander.mp3, CorruptFileError — the 1-of-18 without a subboxId), no longer silent. Library-state note: dev now has all 9 Kodzo present (plan's 1-missing Sammy Virji baseline superseded). No code change (executing test plan). QA Dup Test playlist left on test260526 (idempotently deleted next make-dup run).
2026-07-13 15:10 | coverage: full-page Search (/search/:itemType) client journey — new driver scripts/qa/search-journey.mjs (deep-link `?query=` + real tab-button clicks; live-typing dropped as the collapse-expand magnifier is flaky to automate and SearchContent reads ?query= directly) | verified — Tracks "Hamdi" 4 rows incl. "Damager (Hamdi Edit)"; Albums tab 2 Hamdi albums; Artists tab renders artist card(s); no-match `zzqqxnomatchzz` → grid correctly empty, no crash/error-boundary; empty query → full library list, no crash. Match counts cross-checked vs live Navidrome search3. Rebuilt the dev-mode Electron build first (out/main was stale prod-mode subboxDir="subbox" from 07-10 → now subbox-dev). Distinguished the full-page search from the sidebar cmdk command palette (separate surface). No product fix. Logged 2 ux-notes OPEN (needs-design-call): (a) no "no results" empty-state message on zero matches; (b) uncontrolled SearchInput text goes stale vs results when query changes via nav (not via typing). Writeup: features/search.md. Checked off Search in README.
2026-07-13 20:20 | coverage: Library → Genres (/library/genres, detail) client journey — new driver scripts/qa/genres-journey.mjs (grid → first-card nav → deep-link a rich genre for detail/toggle/play, forcing genreTarget deterministically) | verified end to end, NO bug. Grid renders (18 cards in DOM, 63 genres live). First-card genre ("Dance") deliberately skipped for content checks (only 1 song/1 album) → deep-linked "Electronic" (7bLYq0Np81m1Wgy5N31nuG). Track mode: badge 146 + 13 rows (== live 146 songs); toggle Tracks→Albums flips store+UI, album view badge 60 + 24 cards (== live 60 albums); toggle back to Tracks. Playback: double-click a track row → real Navidrome stream (id LD0Qfpnu7EbSAjY, "Kai Whiston — Quiet as Kept, F.O.G."), audio.paused=false, currentTime advancing. Root-caused two red herrings so they aren't re-chased: (a) genreTarget is a GLOBAL persisted setting (default track) that carries across launches — must be forced deterministically; (b) ag-grid data rows report height=0 in this Playwright build, so a row double-click must aim ~18px below box.y (same quirk as songs-browse). Also: header Play is icon-only (no aria name) and opens a Play/Add-next/Add-last popover, not a direct play — app-wide pattern, not genre-specific. Writeup: features/genres-browse.md. Checked off Genres in README. No product code change; no PR (verification/coverage cycle). Journal drift noted for a future cycle: README "Library — artists" row is unchecked despite artists-browse.md being verified 2026-07-10, and that doc references a ux-notes "role-only artists" entry that is no longer present in ux-notes.md (likely lost in a rebase).
