# Local override for `pnpm prod:web`. Gitignored — see .env.production for why
# this isn't set there. URLs are left untouched so this still authenticates
# against real production, matching how the deployed build behaves.
VITE_DEMO_PASSWORD=demopassword!
